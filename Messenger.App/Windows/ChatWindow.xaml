<Window x:Class="Messenger.App.Windows.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messenger.App.Windows"
        xmlns:controls="clr-namespace:Messenger.App.UserControls.ChatControls"
        mc:Ignorable="d" x:Name="window" Loaded="window_Loaded"
        Title="ChatWindow" Height="450" Width="800">

    <Window.Resources>
        <DataTemplate x:Key="users_ListBoxItem" DataType="{x:Type ListBoxItem}">
            <controls:UserLabelControl UserName="{Binding ReceiverUser.Name}"/>
        </DataTemplate>

        <DataTemplate x:Key="messages_ListBoxItem" DataType="{x:Type ListBoxItem}">
            <TextBlock Text="{Binding Text}"/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="2" BorderThickness="2" BorderBrush="Black" CornerRadius="5"/>
            <Border Grid.Row="1" Margin="2" BorderThickness="2" BorderBrush="Black" CornerRadius="5"/>
            <Border Grid.Row="2" Margin="2" BorderThickness="2" BorderBrush="Black" CornerRadius="5"/>

            <controls:UserLabelControl x:Name="currentUserLabel" Grid.Row="0" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="0" Margin="5" Text="online" HorizontalAlignment="Right" VerticalAlignment="Center"/>

            <ScrollViewer Grid.Row="1" Margin="5">
                <ItemsControl ItemsSource="{Binding ElementName=window, Path=Messages}" ItemTemplate="{StaticResource messages_ListBoxItem}"/>
            </ScrollViewer>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="messageTextBox" Grid.Column="0" Margin="10" Width="Auto"/>
                <Button Grid.Column="1" Content="Send" Margin="20" Click="SendButton_Click"/>
            </Grid>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Margin="2" BorderThickness="2" BorderBrush="Black" CornerRadius="5"/>
            <Border Grid.Row="1" Margin="2" BorderThickness="2" BorderBrush="Black" CornerRadius="5"/>

            <TextBox Grid.Row="0" Margin="10" Width="Auto"/>

            <ScrollViewer Grid.Row="1" Margin="5">
                <ItemsControl ItemsSource="{Binding ElementName=window, Path=Chats}" ItemTemplate="{StaticResource users_ListBoxItem}"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
