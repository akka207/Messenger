<UserControl x:Class="Messenger.App.UserControls.AuthorizeControls.SignUpControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Messenger.App.UserControls.AuthorizeControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Login"/>
                <TextBox x:Name="loginTextBox" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Name"/>
                <TextBox x:Name="nameTextBox" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Password"/>
                <TextBox x:Name="passwordTextBox" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Confirm"/>
                <TextBox x:Name="passwordConfirmTextBox" />
            </StackPanel>
            <Button Content="Submit" Click="Button_SignUp_Click"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Log In?" Click="Button_ChangeMode_Click"/>
    </Grid>
</UserControl>
